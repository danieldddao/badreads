%title Create New Position!

%h3{ :style => "text-align: center;" } Create New Staff or New Admin

%div{ :style => "text-align: center;" }
  -if (@current_user && @current_user.position == "Admin")
    = form_tag new_staff_admin_create_path do
      = label :role, :position, 'Choose Position'
      %div{:style => "width: 8%; display: inline-table;"}
        = select :role, :position, ['Staff', 'Admin'], {}, :class => "form-control"
      %br
      %br
      = submit_tag 'Add New Position', :class => "btn btn-success"
  
    %br
    %br
    %h3 List of created positions that haven't been used yet
    %table#books{ :style => "display: inline-table;" }
      %thead
        %tr
          %th Position
          %th Position Code
      %tbody
        - @roles.each do |role|
          %tr
            %td= role.position 
            %td= role.position_code
            
  -else
    You can't access this page!!