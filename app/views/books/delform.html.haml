%title Delete a Book
%div{ :style => "text-align: center;" }
  %h3 Delete a Book

  -if (@current_user && @current_user.position != "User")
    = form_tag deletebook_path do
      %br
      = label :book, :title, 'Enter Book Title:'
      = text_field :book, 'title'
      %br
      = label :book, :author, 'Enter Book Author:'
      = text_field :book, 'author'
      %br
      %br
      = submit_tag 'Save Changes', :class => "btn btn-danger"
  -else
    Users cannot delete books!!
  end