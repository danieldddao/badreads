%title Book Collection
%div{ :style => "text-align: center;" }
  %h3 All books in our collection
  %table{ :style => "display: inline-table; width: 100%;", :class => "table table-striped table-bordered" }
    %thead
      %tr{ :style => "background-color: wheat;"}
        %th{ :style => "text-align: center;" } ISBN
        %th{ :style => "text-align: center;" } Title
        %th{ :style => "text-align: center;" } Author
        %th{ :style => "text-align: center;" } Publication Year
        %th{ :style => "text-align: center;" } Category
        %th{ :style => "text-align: center;" } Edition
        %th
    %tbody
      - @books.each do |book|
        %tr
          %td= book.isbn 
          %td= book.title
          %td= book.author
          %td= book.publication_year 
          %td= book.category
          %td= book.edition
          %td= link_to "More Information About #{book.title}", book_path(book)
